# automated code reviws using ai tool
it is review for correction error and bug and correct to generate right code 
git add requirements.txt
git commit -m "Add requirements.txt for Render deployment"
git push origin main
